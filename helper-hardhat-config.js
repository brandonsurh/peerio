const { ethers } = require("hardhat");

const networkConfig = {
    3141: {
        name: "Hyperspace",
    },
    314: {
        name: "FilecoinMainnet",
    },
}

module.exports = {
    networkConfig,
}
